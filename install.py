#!/usr/bin/env python


print """pyzgoubi has switched to using the standard python distutils.
to install pyzgoubi into the default directory for your system run:
./setup.py install
(this will require admin access)

To install to you home directory (or else where) use
./setup.py install --prefix ~/pyzgoubi
"""
